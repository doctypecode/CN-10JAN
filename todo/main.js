// event.target - Target is from where the event has been triggered.
// event.preventDefault - Used to stop any kind of default behavior, for example in form submit the default behavior is to reload the page. So use this to prevent that

// Local Storage
import renderTodos from "./ui.js"; //{renderTodos: renderTodos}

// { renderTodos } = {renderTodos: renderTodos}; //{renderTodos: renderTodos}

renderTodos();

//  - default
//  - named

// const data = {
//   name: "ABC",
//   age: 23,
//   city: "Noida",
// };

// const { name: fullName, age, city } = data;

// console.log(fullName, age, city);
